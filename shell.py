class NetShell:

    @staticmethod
    def _info(str,src='NetShell'):
        print(f'{src}:{str}')

    def __init__(self):
        self._info('init')

    def shift(self, target_network):
        return model

    def feed(self, parameter_list):
        pass

ns = NetShell()
ns.feed(dataset)


model = ns.shift('ResNet')
model.load_weight()

model.train_weight()
model.eval()

model.scan('test.png')

model
